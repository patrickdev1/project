/* function pontuar(nota) {
    if(nota >= 90) {
        console.log('A')
    }
    
    if(nota >= 80 && nota <= 89) {
        console.log('B')
    }
    
    if(nota >= 70 && nota <= 79) {
        console.log('C')
    }
    
    if(nota >= 60 && nota <= 69) {
        console.log('D')
    }
    
    if(nota <= 60) {
        console.log('F')
    }
}
const notas = [85, 20, 44, 60, 77, 90, 12, 10]

notas.forEach(nota => pontuar(nota)) */



/* let contas = {
    receitas: [250, 100, 1000],
    despesas: [120, 700, 1500]
}

function calcular(conta) {
    let totalReceita = 0
    conta.receitas.forEach(element => {
        totalReceita = totalReceita + element
    });
    let totalDespesa = 0
    conta.despesas.forEach(element => {
        totalDespesa = totalDespesa + element
    })

    var saldo = totalReceita - totalDespesa

    if (saldo > 0) {
        console.log(`O saldo atual é ${saldo} Positivo`)
    }
    if (saldo <= 0) {
        console.log(`O saldo atual é ${saldo} Negativo`)
    }

}

calcular(contas) */



/* let temperatura = 100


function CelsiusToFahrenheit(celsius) {
    return celsius * 9/5 + 32   
}

function FahrenheitToCelsius(fahrenheit) {
    return fahrenheit - 32 * 5/9
}

let fahrenheit = CelsiusToFahrenheit(temperatura)

let celsius = FahrenheitToCelsius(temperatura)

console.log(celsius, fahrenheit) */